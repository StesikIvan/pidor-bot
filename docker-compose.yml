services:
  pidor-bot:
    build: .
    container_name: pidor-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/pidor.db
